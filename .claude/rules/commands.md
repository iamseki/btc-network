## Commands

```bash
cargo build          # compile
cargo run            # connect to a Bitcoin node, perform handshake, print peer list
cargo check          # fast type-check without producing a binary
cargo clippy         # lint
cargo test           # run all unit tests (29 tests, all in src/wire.rs)
cargo test wire::    # run only the wire module tests
```
