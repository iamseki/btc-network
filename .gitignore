# Rust build output
/target

# Claude Code — personal permission overrides and local settings.
# settings.json (no .local.) is safe to commit for shared project config.
.claude/settings.local.json


*.dat

artifacts