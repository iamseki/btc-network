[package]
name = "btc-network-mmdb"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
readme = "README.md"

[dependencies]
btc-network = { path = "../btc-network" }
maxminddb = "0.27.3"

[dev-dependencies]
maxminddb-writer = "0.1.2"
serde = { version = "1", features = ["derive"] }
