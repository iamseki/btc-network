[package]
name = "btc-network-observability"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "json"] }
